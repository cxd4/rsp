#include "vu.h"

void VADD(int vd, int vs, int vt, int element)
{
    register signed long int sum;

    switch (element)
    {
        case 0x0: /* none:  { 00, 01, 02, 03, 04, 05, 06, 07 } */
        case 0x1:
            sum = VR[vs].s[00] + VR[vt].s[00];
            sum = (VCF[00] & 0x0001) ? ++sum : sum;
            VACC[00].w[01] = (short)sum;
            VR[vd].s[00] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[01] + VR[vt].s[01];
            sum = (VCF[00] & 0x0002) ? ++sum : sum;
            VACC[01].w[01] = (short)sum;
            VR[vd].s[01] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[02] + VR[vt].s[02];
            sum = (VCF[00] & 0x0004) ? ++sum : sum;
            VACC[02].w[01] = (short)sum;
            VR[vd].s[02] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[03] + VR[vt].s[03];
            sum = (VCF[00] & 0x0008) ? ++sum : sum;
            VACC[03].w[01] = (short)sum;
            VR[vd].s[03] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[04] + VR[vt].s[04];
            sum = (VCF[00] & 0x0010) ? ++sum : sum;
            VACC[04].w[01] = (short)sum;
            VR[vd].s[04] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[05] + VR[vt].s[05];
            sum = (VCF[00] & 0x0020) ? ++sum : sum;
            VACC[05].w[01] = (short)sum;
            VR[vd].s[05] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[06] + VR[vt].s[06];
            sum = (VCF[00] & 0x0040) ? ++sum : sum;
            VACC[06].w[01] = (short)sum;
            VR[vd].s[06] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[07] + VR[vt].s[07];
            sum = (VCF[00] & 0x0080) ? ++sum : sum;
            VACC[07].w[01] = (short)sum;
            VR[vd].s[07] = SIGNED_CLAMP(sum);
            VCF[00] = 0x0000;
            return;
        case 0x2: /* 0Q:  { 00, 00, 02, 02, 04, 04, 06, 06 } */
            sum = VR[vs].s[01] + VR[vt].s[00];
            sum = (VCF[00] & 0x0002) ? ++sum : sum;
            VACC[01].w[01] = (short)sum;
            VR[vd].s[01] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[03] + VR[vt].s[02];
            sum = (VCF[00] & 0x0008) ? ++sum : sum;
            VACC[03].w[01] = (short)sum;
            VR[vd].s[03] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[05] + VR[vt].s[04];
            sum = (VCF[00] & 0x0020) ? ++sum : sum;
            VACC[05].w[01] = (short)sum;
            VR[vd].s[05] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[07] + VR[vt].s[06];
            sum = (VCF[00] & 0x0080) ? ++sum : sum;
            VACC[07].w[01] = (short)sum;
            VR[vd].s[07] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[00] + VR[vt].s[00];
            sum = (VCF[00] & 0x0001) ? ++sum : sum;
            VACC[00].w[01] = (short)sum;
            VR[vd].s[00] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[02] + VR[vt].s[02];
            sum = (VCF[00] & 0x0004) ? ++sum : sum;
            VACC[02].w[01] = (short)sum;
            VR[vd].s[02] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[04] + VR[vt].s[04];
            sum = (VCF[00] & 0x0010) ? ++sum : sum;
            VACC[04].w[01] = (short)sum;
            VR[vd].s[04] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[06] + VR[vt].s[06];
            sum = (VCF[00] & 0x0040) ? ++sum : sum;
            VACC[06].w[01] = (short)sum;
            VR[vd].s[06] = SIGNED_CLAMP(sum);
            VCF[00] = 0x0000;
            return;
        case 0x3: /* 1Q:  { 01, 01, 03, 03, 05, 05, 07, 07 } */
            sum = VR[vs].s[00] + VR[vt].s[01];
            sum = (VCF[00] & 0x0001) ? ++sum : sum;
            VACC[00].w[01] = (short)sum;
            VR[vd].s[00] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[02] + VR[vt].s[03];
            sum = (VCF[00] & 0x0004) ? ++sum : sum;
            VACC[02].w[01] = (short)sum;
            VR[vd].s[02] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[04] + VR[vt].s[05];
            sum = (VCF[00] & 0x0010) ? ++sum : sum;
            VACC[04].w[01] = (short)sum;
            VR[vd].s[04] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[06] + VR[vt].s[07];
            sum = (VCF[00] & 0x0040) ? ++sum : sum;
            VACC[06].w[01] = (short)sum;
            VR[vd].s[06] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[01] + VR[vt].s[01];
            sum = (VCF[00] & 0x0002) ? ++sum : sum;
            VACC[01].w[01] = (short)sum;
            VR[vd].s[01] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[03] + VR[vt].s[03];
            sum = (VCF[00] & 0x0008) ? ++sum : sum;
            VACC[03].w[01] = (short)sum;
            VR[vd].s[03] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[05] + VR[vt].s[05];
            sum = (VCF[00] & 0x0020) ? ++sum : sum;
            VACC[05].w[01] = (short)sum;
            VR[vd].s[05] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[07] + VR[vt].s[07];
            sum = (VCF[00] & 0x0080) ? ++sum : sum;
            VACC[07].w[01] = (short)sum;
            VR[vd].s[07] = SIGNED_CLAMP(sum);
            VCF[00] = 0x0000;
            return;
        case 0x4: /* 0H:  { 00, 00, 00, 00, 04, 04, 04, 04 } */
            sum = VR[vs].s[01] + VR[vt].s[00];
            sum = (VCF[00] & 0x0002) ? ++sum : sum;
            VACC[01].w[01] = (short)sum;
            VR[vd].s[01] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[02] + VR[vt].s[00];
            sum = (VCF[00] & 0x0004) ? ++sum : sum;
            VACC[02].w[01] = (short)sum;
            VR[vd].s[02] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[03] + VR[vt].s[00];
            sum = (VCF[00] & 0x0008) ? ++sum : sum;
            VACC[03].w[01] = (short)sum;
            VR[vd].s[03] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[05] + VR[vt].s[04];
            sum = (VCF[00] & 0x0020) ? ++sum : sum;
            VACC[05].w[01] = (short)sum;
            VR[vd].s[05] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[06] + VR[vt].s[04];
            sum = (VCF[00] & 0x0040) ? ++sum : sum;
            VACC[06].w[01] = (short)sum;
            VR[vd].s[06] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[07] + VR[vt].s[04];
            sum = (VCF[00] & 0x0080) ? ++sum : sum;
            VACC[07].w[01] = (short)sum;
            VR[vd].s[07] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[00] + VR[vt].s[00];
            sum = (VCF[00] & 0x0001) ? ++sum : sum;
            VACC[00].w[01] = (short)sum;
            VR[vd].s[00] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[04] + VR[vt].s[04];
            sum = (VCF[00] & 0x0010) ? ++sum : sum;
            VACC[04].w[01] = (short)sum;
            VR[vd].s[04] = SIGNED_CLAMP(sum);
            VCF[00] = 0x0000;
            return;
        case 0x5: /* 1H:  { 01, 01, 01, 01, 05, 05, 05, 05 } */
            sum = VR[vs].s[00] + VR[vt].s[01];
            sum = (VCF[00] & 0x0001) ? ++sum : sum;
            VACC[00].w[01] = (short)sum;
            VR[vd].s[00] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[02] + VR[vt].s[01];
            sum = (VCF[00] & 0x0004) ? ++sum : sum;
            VACC[02].w[01] = (short)sum;
            VR[vd].s[02] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[03] + VR[vt].s[01];
            sum = (VCF[00] & 0x0008) ? ++sum : sum;
            VACC[03].w[01] = (short)sum;
            VR[vd].s[03] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[04] + VR[vt].s[05];
            sum = (VCF[00] & 0x0010) ? ++sum : sum;
            VACC[04].w[01] = (short)sum;
            VR[vd].s[04] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[06] + VR[vt].s[05];
            sum = (VCF[00] & 0x0040) ? ++sum : sum;
            VACC[06].w[01] = (short)sum;
            VR[vd].s[06] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[07] + VR[vt].s[05];
            sum = (VCF[00] & 0x0080) ? ++sum : sum;
            VACC[07].w[01] = (short)sum;
            VR[vd].s[07] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[01] + VR[vt].s[01];
            sum = (VCF[00] & 0x0002) ? ++sum : sum;
            VACC[01].w[01] = (short)sum;
            VR[vd].s[01] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[05] + VR[vt].s[05];
            sum = (VCF[00] & 0x0020) ? ++sum : sum;
            VACC[05].w[01] = (short)sum;
            VR[vd].s[05] = SIGNED_CLAMP(sum);
            VCF[00] = 0x0000;
            return;
        case 0x6: /* 2H:  { 02, 02, 02, 02, 06, 06, 06, 06 } */
            sum = VR[vs].s[00] + VR[vt].s[02];
            sum = (VCF[00] & 0x0001) ? ++sum : sum;
            VACC[00].w[01] = (short)sum;
            VR[vd].s[00] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[01] + VR[vt].s[02];
            sum = (VCF[00] & 0x0002) ? ++sum : sum;
            VACC[01].w[01] = (short)sum;
            VR[vd].s[01] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[03] + VR[vt].s[02];
            sum = (VCF[00] & 0x0008) ? ++sum : sum;
            VACC[03].w[01] = (short)sum;
            VR[vd].s[03] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[04] + VR[vt].s[06];
            sum = (VCF[00] & 0x0010) ? ++sum : sum;
            VACC[04].w[01] = (short)sum;
            VR[vd].s[04] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[05] + VR[vt].s[06];
            sum = (VCF[00] & 0x0020) ? ++sum : sum;
            VACC[05].w[01] = (short)sum;
            VR[vd].s[05] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[07] + VR[vt].s[06];
            sum = (VCF[00] & 0x0080) ? ++sum : sum;
            VACC[07].w[01] = (short)sum;
            VR[vd].s[07] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[02] + VR[vt].s[02];
            sum = (VCF[00] & 0x0004) ? ++sum : sum;
            VACC[02].w[01] = (short)sum;
            VR[vd].s[02] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[06] + VR[vt].s[06];
            sum = (VCF[00] & 0x0040) ? ++sum : sum;
            VACC[06].w[01] = (short)sum;
            VR[vd].s[06] = SIGNED_CLAMP(sum);
            VCF[00] = 0x0000;
            return;
        case 0x7: /* 3H:  { 03, 03, 03, 03, 07, 07, 07, 07 } */
            sum = VR[vs].s[00] + VR[vt].s[03];
            sum = (VCF[00] & 0x0001) ? ++sum : sum;
            VACC[00].w[01] = (short)sum;
            VR[vd].s[00] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[01] + VR[vt].s[03];
            sum = (VCF[00] & 0x0002) ? ++sum : sum;
            VACC[01].w[01] = (short)sum;
            VR[vd].s[01] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[02] + VR[vt].s[03];
            sum = (VCF[00] & 0x0004) ? ++sum : sum;
            VACC[02].w[01] = (short)sum;
            VR[vd].s[02] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[04] + VR[vt].s[07];
            sum = (VCF[00] & 0x0010) ? ++sum : sum;
            VACC[04].w[01] = (short)sum;
            VR[vd].s[04] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[05] + VR[vt].s[07];
            sum = (VCF[00] & 0x0020) ? ++sum : sum;
            VACC[05].w[01] = (short)sum;
            VR[vd].s[05] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[06] + VR[vt].s[07];
            sum = (VCF[00] & 0x0040) ? ++sum : sum;
            VACC[06].w[01] = (short)sum;
            VR[vd].s[06] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[03] + VR[vt].s[03];
            sum = (VCF[00] & 0x0008) ? ++sum : sum;
            VACC[03].w[01] = (short)sum;
            VR[vd].s[03] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[07] + VR[vt].s[07];
            sum = (VCF[00] & 0x0080) ? ++sum : sum;
            VACC[07].w[01] = (short)sum;
            VR[vd].s[07] = SIGNED_CLAMP(sum);
            VCF[00] = 0x0000;
            return;
        case 0x8: /* 0:  { 00, 00, 00, 00, 00, 00, 00, 00 } */
            sum = VR[vs].s[01] + VR[vt].s[00];
            sum = (VCF[00] & 0x0002) ? ++sum : sum;
            VACC[01].w[01] = (short)sum;
            VR[vd].s[01] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[02] + VR[vt].s[00];
            sum = (VCF[00] & 0x0004) ? ++sum : sum;
            VACC[02].w[01] = (short)sum;
            VR[vd].s[02] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[03] + VR[vt].s[00];
            sum = (VCF[00] & 0x0008) ? ++sum : sum;
            VACC[03].w[01] = (short)sum;
            VR[vd].s[03] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[04] + VR[vt].s[00];
            sum = (VCF[00] & 0x0010) ? ++sum : sum;
            VACC[04].w[01] = (short)sum;
            VR[vd].s[04] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[05] + VR[vt].s[00];
            sum = (VCF[00] & 0x0020) ? ++sum : sum;
            VACC[05].w[01] = (short)sum;
            VR[vd].s[05] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[06] + VR[vt].s[00];
            sum = (VCF[00] & 0x0040) ? ++sum : sum;
            VACC[06].w[01] = (short)sum;
            VR[vd].s[06] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[07] + VR[vt].s[00];
            sum = (VCF[00] & 0x0080) ? ++sum : sum;
            VACC[07].w[01] = (short)sum;
            VR[vd].s[07] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[00] + VR[vt].s[00];
            sum = (VCF[00] & 0x0001) ? ++sum : sum;
            VACC[00].w[01] = (short)sum;
            VR[vd].s[00] = SIGNED_CLAMP(sum);
            VCF[00] = 0x0000;
            return;
        case 0x9: /* 1:  { 01, 01, 01, 01, 01, 01, 01, 01 } */
            sum = VR[vs].s[00] + VR[vt].s[01];
            sum = (VCF[00] & 0x0001) ? ++sum : sum;
            VACC[00].w[01] = (short)sum;
            VR[vd].s[00] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[02] + VR[vt].s[01];
            sum = (VCF[00] & 0x0004) ? ++sum : sum;
            VACC[02].w[01] = (short)sum;
            VR[vd].s[02] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[03] + VR[vt].s[01];
            sum = (VCF[00] & 0x0008) ? ++sum : sum;
            VACC[03].w[01] = (short)sum;
            VR[vd].s[03] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[04] + VR[vt].s[01];
            sum = (VCF[00] & 0x0010) ? ++sum : sum;
            VACC[04].w[01] = (short)sum;
            VR[vd].s[04] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[05] + VR[vt].s[01];
            sum = (VCF[00] & 0x0020) ? ++sum : sum;
            VACC[05].w[01] = (short)sum;
            VR[vd].s[05] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[06] + VR[vt].s[01];
            sum = (VCF[00] & 0x0040) ? ++sum : sum;
            VACC[06].w[01] = (short)sum;
            VR[vd].s[06] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[07] + VR[vt].s[01];
            sum = (VCF[00] & 0x0080) ? ++sum : sum;
            VACC[07].w[01] = (short)sum;
            VR[vd].s[07] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[01] + VR[vt].s[01];
            sum = (VCF[00] & 0x0002) ? ++sum : sum;
            VACC[01].w[01] = (short)sum;
            VR[vd].s[01] = SIGNED_CLAMP(sum);
            VCF[00] = 0x0000;
            return;
        case 0xA: /* 2:  { 02, 02, 02, 02, 02, 02, 02, 02 } */
            sum = VR[vs].s[00] + VR[vt].s[02];
            sum = (VCF[00] & 0x0001) ? ++sum : sum;
            VACC[00].w[01] = (short)sum;
            VR[vd].s[00] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[01] + VR[vt].s[02];
            sum = (VCF[00] & 0x0002) ? ++sum : sum;
            VACC[01].w[01] = (short)sum;
            VR[vd].s[01] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[03] + VR[vt].s[02];
            sum = (VCF[00] & 0x0008) ? ++sum : sum;
            VACC[03].w[01] = (short)sum;
            VR[vd].s[03] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[04] + VR[vt].s[02];
            sum = (VCF[00] & 0x0010) ? ++sum : sum;
            VACC[04].w[01] = (short)sum;
            VR[vd].s[04] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[05] + VR[vt].s[02];
            sum = (VCF[00] & 0x0020) ? ++sum : sum;
            VACC[05].w[01] = (short)sum;
            VR[vd].s[05] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[06] + VR[vt].s[02];
            sum = (VCF[00] & 0x0040) ? ++sum : sum;
            VACC[06].w[01] = (short)sum;
            VR[vd].s[06] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[07] + VR[vt].s[02];
            sum = (VCF[00] & 0x0080) ? ++sum : sum;
            VACC[07].w[01] = (short)sum;
            VR[vd].s[07] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[02] + VR[vt].s[02];
            sum = (VCF[00] & 0x0004) ? ++sum : sum;
            VACC[02].w[01] = (short)sum;
            VR[vd].s[02] = SIGNED_CLAMP(sum);
            VCF[00] = 0x0000;
            return;
        case 0xB: /* 3:  { 03, 03, 03, 03, 03, 03, 03, 03 } */
            sum = VR[vs].s[00] + VR[vt].s[03];
            sum = (VCF[00] & 0x0001) ? ++sum : sum;
            VACC[00].w[01] = (short)sum;
            VR[vd].s[00] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[01] + VR[vt].s[03];
            sum = (VCF[00] & 0x0002) ? ++sum : sum;
            VACC[01].w[01] = (short)sum;
            VR[vd].s[01] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[02] + VR[vt].s[03];
            sum = (VCF[00] & 0x0004) ? ++sum : sum;
            VACC[02].w[01] = (short)sum;
            VR[vd].s[02] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[04] + VR[vt].s[03];
            sum = (VCF[00] & 0x0010) ? ++sum : sum;
            VACC[04].w[01] = (short)sum;
            VR[vd].s[04] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[05] + VR[vt].s[03];
            sum = (VCF[00] & 0x0020) ? ++sum : sum;
            VACC[05].w[01] = (short)sum;
            VR[vd].s[05] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[06] + VR[vt].s[03];
            sum = (VCF[00] & 0x0040) ? ++sum : sum;
            VACC[06].w[01] = (short)sum;
            VR[vd].s[06] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[07] + VR[vt].s[03];
            sum = (VCF[00] & 0x0080) ? ++sum : sum;
            VACC[07].w[01] = (short)sum;
            VR[vd].s[07] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[03] + VR[vt].s[03];
            sum = (VCF[00] & 0x0008) ? ++sum : sum;
            VACC[03].w[01] = (short)sum;
            VR[vd].s[03] = SIGNED_CLAMP(sum);
            VCF[00] = 0x0000;
            return;
        case 0xC: /* 4:  { 04, 04, 04, 04, 04, 04, 04, 04 } */
            sum = VR[vs].s[00] + VR[vt].s[04];
            sum = (VCF[00] & 0x0001) ? ++sum : sum;
            VACC[00].w[01] = (short)sum;
            VR[vd].s[00] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[01] + VR[vt].s[04];
            sum = (VCF[00] & 0x0002) ? ++sum : sum;
            VACC[01].w[01] = (short)sum;
            VR[vd].s[01] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[02] + VR[vt].s[04];
            sum = (VCF[00] & 0x0004) ? ++sum : sum;
            VACC[02].w[01] = (short)sum;
            VR[vd].s[02] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[03] + VR[vt].s[04];
            sum = (VCF[00] & 0x0008) ? ++sum : sum;
            VACC[03].w[01] = (short)sum;
            VR[vd].s[03] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[05] + VR[vt].s[04];
            sum = (VCF[00] & 0x0020) ? ++sum : sum;
            VACC[05].w[01] = (short)sum;
            VR[vd].s[05] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[06] + VR[vt].s[04];
            sum = (VCF[00] & 0x0040) ? ++sum : sum;
            VACC[06].w[01] = (short)sum;
            VR[vd].s[06] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[07] + VR[vt].s[04];
            sum = (VCF[00] & 0x0080) ? ++sum : sum;
            VACC[07].w[01] = (short)sum;
            VR[vd].s[07] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[04] + VR[vt].s[04];
            sum = (VCF[00] & 0x0010) ? ++sum : sum;
            VACC[04].w[01] = (short)sum;
            VR[vd].s[04] = SIGNED_CLAMP(sum);
            VCF[00] = 0x0000;
            return;
        case 0xD: /* 5:  { 05, 05, 05, 05, 05, 05, 05, 05 } */
            sum = VR[vs].s[00] + VR[vt].s[05];
            sum = (VCF[00] & 0x0001) ? ++sum : sum;
            VACC[00].w[01] = (short)sum;
            VR[vd].s[00] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[01] + VR[vt].s[05];
            sum = (VCF[00] & 0x0002) ? ++sum : sum;
            VACC[01].w[01] = (short)sum;
            VR[vd].s[01] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[02] + VR[vt].s[05];
            sum = (VCF[00] & 0x0004) ? ++sum : sum;
            VACC[02].w[01] = (short)sum;
            VR[vd].s[02] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[03] + VR[vt].s[05];
            sum = (VCF[00] & 0x0008) ? ++sum : sum;
            VACC[03].w[01] = (short)sum;
            VR[vd].s[03] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[04] + VR[vt].s[05];
            sum = (VCF[00] & 0x0010) ? ++sum : sum;
            VACC[04].w[01] = (short)sum;
            VR[vd].s[04] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[06] + VR[vt].s[05];
            sum = (VCF[00] & 0x0040) ? ++sum : sum;
            VACC[06].w[01] = (short)sum;
            VR[vd].s[06] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[07] + VR[vt].s[05];
            sum = (VCF[00] & 0x0080) ? ++sum : sum;
            VACC[07].w[01] = (short)sum;
            VR[vd].s[07] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[05] + VR[vt].s[05];
            sum = (VCF[00] & 0x0020) ? ++sum : sum;
            VACC[05].w[01] = (short)sum;
            VR[vd].s[05] = SIGNED_CLAMP(sum);
            VCF[00] = 0x0000;
            return;
        case 0xE: /* 6:  { 06, 06, 06, 06, 06, 06, 06, 06 } */
            sum = VR[vs].s[00] + VR[vt].s[06];
            sum = (VCF[00] & 0x0001) ? ++sum : sum;
            VACC[00].w[01] = (short)sum;
            VR[vd].s[00] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[01] + VR[vt].s[06];
            sum = (VCF[00] & 0x0002) ? ++sum : sum;
            VACC[01].w[01] = (short)sum;
            VR[vd].s[01] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[02] + VR[vt].s[06];
            sum = (VCF[00] & 0x0004) ? ++sum : sum;
            VACC[02].w[01] = (short)sum;
            VR[vd].s[02] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[03] + VR[vt].s[06];
            sum = (VCF[00] & 0x0008) ? ++sum : sum;
            VACC[03].w[01] = (short)sum;
            VR[vd].s[03] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[04] + VR[vt].s[06];
            sum = (VCF[00] & 0x0010) ? ++sum : sum;
            VACC[04].w[01] = (short)sum;
            VR[vd].s[04] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[05] + VR[vt].s[06];
            sum = (VCF[00] & 0x0020) ? ++sum : sum;
            VACC[05].w[01] = (short)sum;
            VR[vd].s[05] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[07] + VR[vt].s[06];
            sum = (VCF[00] & 0x0080) ? ++sum : sum;
            VACC[07].w[01] = (short)sum;
            VR[vd].s[07] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[06] + VR[vt].s[06];
            sum = (VCF[00] & 0x0040) ? ++sum : sum;
            VACC[06].w[01] = (short)sum;
            VR[vd].s[06] = SIGNED_CLAMP(sum);
            VCF[00] = 0x0000;
            return;
        case 0xF: /* 7:  { 07, 07, 07, 07, 07, 07, 07, 07 } */
            sum = VR[vs].s[00] + VR[vt].s[07];
            sum = (VCF[00] & 0x0001) ? ++sum : sum;
            VACC[00].w[01] = (short)sum;
            VR[vd].s[00] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[01] + VR[vt].s[07];
            sum = (VCF[00] & 0x0002) ? ++sum : sum;
            VACC[01].w[01] = (short)sum;
            VR[vd].s[01] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[02] + VR[vt].s[07];
            sum = (VCF[00] & 0x0004) ? ++sum : sum;
            VACC[02].w[01] = (short)sum;
            VR[vd].s[02] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[03] + VR[vt].s[07];
            sum = (VCF[00] & 0x0008) ? ++sum : sum;
            VACC[03].w[01] = (short)sum;
            VR[vd].s[03] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[04] + VR[vt].s[07];
            sum = (VCF[00] & 0x0010) ? ++sum : sum;
            VACC[04].w[01] = (short)sum;
            VR[vd].s[04] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[05] + VR[vt].s[07];
            sum = (VCF[00] & 0x0020) ? ++sum : sum;
            VACC[05].w[01] = (short)sum;
            VR[vd].s[05] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[06] + VR[vt].s[07];
            sum = (VCF[00] & 0x0040) ? ++sum : sum;
            VACC[06].w[01] = (short)sum;
            VR[vd].s[06] = SIGNED_CLAMP(sum);
            sum = VR[vs].s[07] + VR[vt].s[07];
            sum = (VCF[00] & 0x0080) ? ++sum : sum;
            VACC[07].w[01] = (short)sum;
            VR[vd].s[07] = SIGNED_CLAMP(sum);
            VCF[00] = 0x0000;
            return;
    }
}
